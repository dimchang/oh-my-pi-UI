# 评审：20260925issue.md「会话卡运行中 25h」方案

日期：2026-09-25　评审对象：`20260925issue.md`（bet_zp `01a0cc6c` 事故）
代码基线：`57ef3ef`，`package.json` version = **0.5.20**

## 结论

**方向正确、根因定位准确、可以实施**——但按原文落地会在**它自己要修的那个场景里失效**。必须先改 3 处判据，否则 P0 探针会误报「活着」、P2 灰橙点在事故态下永远不亮。

一句话总评：根因分析（防线 3 的静默盲区）经代码核对**完全属实**；修复方案里 P0 的**思路**对、**机制**选错了（多了个 IPC，却在核心场景判不准）；P1 有价值但有个会让它白干的调用顺序陷阱；P2 的判据自依赖；P3 有个前置 bug 没列。

验收标准第 2 条（手测杀进程树）**测不到目标路径**，需要重写。

> **2026-09-25 二轮（回应反对意见后，已据实测日志修订）**：事故当天诊断日志与会话 JSONL 均完整留存，据此定案——
> **「approve 断帧流」否证**（批准后渲染层实收 22,026 帧）；我提出的「迁移覆写」新假设也**自我证伪**（迁移在事故前 9 小时已完成）。
> 真断点在 `agent_end` 帧到达之后、`isStreaming` 未复位处。详见 §1·补。技术性结论（P0/P1/P2/P3）**不变**，且新增一条 P1 阈值需实测的提示。

---

## 一、根因分析核对：属实

文档对三层防线的定位，逐条对代码验证：

| 文档断言 | 代码位置 | 核对结果 |
|---|---|---|
| 防线 3 的 `if (!r.success) return` 静默放弃 | `App.tsx:1541` | ✅ 属实，裸 `return`，无计数无日志 |
| 防线 3 的 `catch{}` 静默（:1567） | `App.tsx:1567-1569` | ✅ 属实，注释「交给 onExit 路径处理」 |
| 防线 2 onExit 复位 isStreaming | `App.tsx:487` `setProcState(..., {status:'offline', isStreaming:false, ...})` | ✅ 属实 |
| 看门狗只扫 `isStreaming` 的会话 | `App.tsx:769` `if (!ps.isStreaming) continue` | ✅ 事故态 `isStreaming` 卡 true → 确实每 30s 进入对账 |
| 事故态 `status` 卡 `online` → 走 `reconcileStreamingState` 分支 | `App.tsx:770-773` | ✅ 属实 |
| 「防线 3 的设计前提是 onExit 必达」 | `App.tsx:1568` 注释原文 | ✅ 属实 |

**本质判断成立**：进程死后 `rpc.getState` 抛错 → `catch` 静默 `return` → 每 30s 失败一次放弃一次，无限循环。这条链路在代码里是闭合的，无需再验证。

### 一处需修正的归因（已用实测日志定案，不再靠推断）

> 防线 1：帧流在**权限批准后断裂**（PermissionModal approve 路径）

**先认错**：原评审「与后续 15 分钟正常工作不能同时成立」推理过头。omp 服务端存活 ≠ 渲染层收到帧，两者确属独立层面，不能从服务端收尾直接推出渲染层帧流未断——这一点对方是对的，我无权那样推。

**但结论仍然成立，现在有实测证据**。`diagnostics.ts` 心跳每秒记录 `ompStat.frames`（`store.ts:649`，在 `applyAgentEvent` 内自增，即**渲染层实际收到并处理**的帧数）。事故当天日志完整留存于
`AppData/Roaming/omp-gui/logs/ui-2026-09-23.log`（时间戳为 UTC，本地 = UTC+8）：

| UTC | 本地 | frames/min |
|---|---|---|
| 13:27 | 21:27（弹窗 + 点同意） | 1897 |
| 13:28–13:31 | 21:28–21:31 | 1881 / 6 / 1379 / 1433 |
| 13:35–13:39 | 21:35–21:39 | 3497 / 3044 / 1464 / 3159 / 2539 |
| 13:42:44–45 | 21:42:44–45 | 41 → **57**（renders 41 → 58）|
| 13:43–13:44 | 21:43–21:44 | **0** |

批准后到收尾（21:27→21:42）渲染层共收到 **22,026 帧**。若帧订阅真在 approve 处断了，这 16 分钟计数必为 0 —— **「approve 断帧流」被直接否证**。

时间特征恰恰相反：帧流全程正常，在 **21:42:44–45（正是文档记载的服务端收尾时刻）出现最后一波 57 帧爆发，随后归零**。断点在回合收尾的最后几帧，不在 approve。

---

## 一·补：帧到了却没生效 —— 断点比原判据晚得多，且**不是**迁移覆写（假设已证伪）

既然 22,026 帧确实到了渲染层、最后一波就落在收尾时刻，就该问「为什么到了却没生效」。我提出过一个看似漂亮的假设，现在**自己证伪它**：

**假设（已证伪）**：`migrateTempSession` 用陈旧快照覆写 ProcState —— 它在 `App.tsx:209` 取 `st = useApp.getState()` 快照，`:238` `await` 之后才在 `:290` 读 `st.procStateMap[cur]`，于是把 `agent_end` 刚置为 `false` 的 `isStreaming` 用陈旧值 `true` 覆写回 `realPath`（`:296`、`:309`）。看起来完美解释「帧到了但状态没复位 + 内容回退 + 历史重载才吐出」。

**证伪证据**（事故会话 JSONL 实测，`~/.omp/agent/sessions/--D--code-bet_zp--/2026-09-23T04-01-14-069Z_01a0cc6c….jsonl`）：

| 行 | 时刻 (UTC) | 内容 |
|---|---|---|
| 1 | 04:01:14 | session header（自动化 12:01 本地拉起） |
| 6 | 04:01:14 | **第 1 条 user 消息**（自动化 prompt） |
| 41 | **04:04:42** | `stopReason=stop` ← **首轮就已收尾** |
| 42 | 13:23:36 | **第 2 条 user 消息**（用户 21:23 本地发言） |
| 128 | 13:41:24 | `stopReason=stop` |
| 182 | **13:42:45** | `stopReason=stop` ← 文档记载的收尾时刻 |
| 183 | 09-24 14:51:40 | 第 3 条 user `?` |

首轮在 **04:04:42（本地 12:04）** 就 `stop` 并触发 `migrateTempSession`（`App.tsx:433`），即**迁移在事故前 9 小时已完成**。21:42 时该会话早已是 `realPath`、非 `__new_` tempKey → 迁移覆写**不可能发生**。

补充观察（**不确定，勿当结论**）：文件 mtime = **2026-09-24 22:51**（发 `?` 之后），而 21:42:45 的 `stop` 内容确实已在文件中（第 182 行）。mtime 只反映**最后一次**写入，无法据此推断 21:42 那次 flush 的时点——可能是收尾时落盘、也可能是 22:51 新进程 resume 时回写。因此**不能**断言「omp 收尾时未 flush」。

这一条对 P1 有直接影响：P1 的磁盘判据（mtime 距今 >10min 且尾部 `stopReason:'stop'`）在 21:52 能否成立，取决于 21:42 是否落盘 —— **实测无法判定**。若当时未落盘，P1 对**本例**同样救不了（与我 §3.3 说的「omp 只在 agent_end 才 flush」在此例可能不成立是同一个未知）。建议实施时用一个带落盘时刻的探针实测一次再定 P1 阈值。

**结论**：真断点比我和原方案双方的判据都**晚**——不在 approve（帧流全程通畅），不在迁移（早已完成），而在 **21:42:45 之后 `isStreaming` 未复位**这一段。可确定的只有：

- 帧流 21:27–21:42 全程正常（22,026 帧为证）→ **approve 断帧流：否证**；
- 最后一波帧（13:42:44–45，41→57 帧）正落在服务端收尾 → 帧**到达**了渲染层；
- 此后 25h 静默，`isStreaming` 未复位 → 断点在 **`agent_end` 帧的处理/传送最后一步**，或 **`exit` 帧缺失**（两者都能解释，日志无法进一步区分）。

原方案「把传送层三层排查列入待查」因此**保持有效且应保留**，我原评审建议移开 `PermissionModal.tsx:80` 的意见**撤回**：改为「approve 路径已否证，排查聚焦 **agent_end 帧为何未复位 isStreaming** + **exit 帧是否到达**」。

> 注：`migrateTempSession` 的陈旧快照隐患（`:209/238/290/309`）**本身仍是真 bug** —— 它会在「temp 会话首轮 `agent_end`」场景下把状态覆写为陈旧值（本次事故只是恰好不在那个场景）。建议仍修，但**不列为本次事故的成因**。

---

## 二、P0：思路对，机制选错（**阻塞项**）

### 2.1 探针的判据在核心场景会误报「活着」

方案 2 写的是「按 sessionPath 检查 ChildProcess 实况（pid 存活 + exit 是否已挂）」。但实现时最容易写成「查 pool 里有没有这个 entry」：

```ts
// electron/omp-pool.ts:92
get(sessionPath) { const e = this.entries.get(sessionPath); return e && e.status === 'online' ? e : undefined; }
```

问题在这：entry 从 `entries` 里被删掉，**只有** onExit / evict / spawn 失败三条路径（`omp-pool.ts:379`）。也就是说：

- 若主进程的 `child.on('exit')` **真的没触发** → entry 仍在、`status` 仍是 `online`、`pid` 仍存在 → 探针返回 `{alive:true}` → **P0 完全失效**；
- 而这恰恰就是本次事故需要修的场景。

Node 的 `ChildProcess` 在 stdio 管道关闭后 `exit` 必然 fire，所以「主进程完全没感知」概率很低；但**如果它真的没感知，探针也一定跟着没感知**——探针和 onExit 共用同一个真相源，算不上方案说的「不依赖 omp 配合的独立真相源」。

**修正**：探针必须显式查 `ChildProcess` 的终态字段，而不是查池：

```ts
// OmpProcess 需暴露：
get exited(): boolean {
  return this.child === null || this.child.exitCode !== null || this.child.signalCode !== null;
}
```

并且三态返回（不能只有 `alive` 布尔）：

| 返回 | 含义 | 渲染层动作 |
|---|---|---|
| `{ state: 'absent' }` | 池里没有该 entry（已被 onExit/evict 摘除） | **立即清理**，无需计数 |
| `{ state: 'dead', pid }` | entry 在，但 `exitCode/signalCode` 已挂 | **立即清理** |
| `{ state: 'alive', pid }` | 进程真的在跑 | 不动，交给 P1 |

### 2.2 更快且更小的修法：`rpc.getState` 的失败原因已经自带答案

`main.ts:441` → `pool.send` 在 entry 不存在时**立即 throw** `omp process not online for session: ...`（`omp-pool.ts:206`），被 `main.ts:443` 包成 `{ __rpcError }`，再由 `rpc-client.ts:25-27` 还原成 throw，最终落进 `App.tsx:1567` 的 `catch`。

**这个 catch 已经在告诉我们「进程不在池里」了。** 「不在池里」= onExit 已经到达过主进程 = 进程确定已死。所以最小修法是按错误分类，而不是新开 IPC：

```ts
// App.tsx reconcileStreamingState 的 catch
} catch (e) {
  const msg = e instanceof Error ? e.message : String(e);
  if (msg.includes('not online')) {
    // 进程已不在池：权威死亡证据，直接复用 onExit 清理，不必等 90s
    clearSessionProc(sessionPath, 'lost');
    return;
  }
  reconFailCount++;   // 其余（RPC 超时等）才计数
}
```

对比：

| | 原方案（新 IPC 探针 + 计数 ≥3） | 修正版（错误分类为主 + 探针兜底） |
|---|---|---|
| 改动文件 | ipc-channels / preload / main / omp-pool / rpc-client / App | 仅 `App.tsx` + `store.ts` |
| 恢复时延 | ~90s | **≤30s**（下一次看门狗 tick 即刻） |
| 核心场景是否判准 | 依赖探针实现细节，易误报活 | ✅ 直接命中 |
| 覆盖「进程在但 RPC 挂」 | ✅ | ❌ → 由 P1 覆盖 |

建议：**先落修正版（~25 行）**，探针作为后置，只用来覆盖「entry 在、进程僵死、RPC 无响应」这一窄场景。原方案把窄场景当主场景，主次颠倒了。

### 2.3 计数方案的一个隐藏前提不成立

「连续 ≥3 次（约 90s）」假设每次失败是快速失败。但 `FrameRouter` 默认超时是 **5 分钟**（`frame-router.ts:8`），且 `rpc.getState` 没暴露 `timeoutMs` 参数。进程僵死时每次 `getState` 会挂满 5 分钟才 reject，30s 一次会堆积约 10 个 pending，「90s」不成立。

顺带一提：`not online` 是**即时** throw，不进 pending，所以 2.2 的修正版不受此影响——又一个偏向它的理由。若保留计数路径，务必给对账用的 `getState` 传短超时（如 3s）。

### 2.4 方案 3「抽取 `clearSessionProc` 复用」——✅ 可行

`onExit` 的清理体（`App.tsx:487-491`）就是 `setProcState({status:'offline', isStreaming:false, isAborting:false})` + 当前会话的 `setOmpExited`，抽成公共函数无副作用。**注意别把 tempKey 迁移分支（:496-515）和自动 respawn 分支（:518-541）也一并抽进去**——失联清理不该触发 respawn（会重复 `acquire` 出一个新进程，而方案明确要「下次 prompt 走既有 acquire 路径」）。

「下次 prompt 走既有 acquire」已验证存在：`onSend` → `resolveSessionKey` → `rpc.acquire(key, cwd, approvalMode)`（`App.tsx:649-650`）。✅

---

## 三、P1：有价值，但有个会让它白干的顺序陷阱

### 3.1 「先清 isStreaming，再 loadSessionMessages」——顺序反了就完全不生效

`loadSessionMessages` 有竞态守卫（`store.ts:1199-1203`）：

```ts
const framesArrivedDuringRead = (psNow?.lastFrameAt ?? 0) > readStartFrameAt || Boolean(psNow?.isStreaming);
if (existing && existing.length > 0 && framesArrivedDuringRead) {
  pushFrameDiag({ type: 'loadSessionMessages.skipStale', ... });
  return;  // 快照已过时 → 放弃覆盖
}
```

`Boolean(psNow?.isStreaming)` 这一项：只要 `isStreaming` 还是 `true`，**重载必定被丢弃**。若写成「先 load 再清 isStreaming」（或同一个 tick 里顺序颠倒），P1 一行效果都没有——而事故现象「积压内容一次性吐出」正是靠重载才出现的。

必须严格：
```ts
setProcState(path, { isStreaming: false, isAborting: false, stuckSince: undefined });
// 下一行才 load
loadSessionMessages(path);
```

### 3.2 mtime 不用新 IPC，尾部 stopReason 建议单独开一个轻量接口

- **mtime 可以白嫖**：`SessionSummary` 已带 `mtime`（`session-store.ts:155`），渲染层 `store.sessions` 里就有。看门狗直接查 `st.sessions.find(x => x.path === p)?.mtime`，零新增 IPC。
- **尾部 stopReason 需要新接口**，但不要复用 `readSessionMessages`：它是全量流式读（`session-store.ts:221`），卡死会话每 30s 全量读一次数十 MB 的 JSONL 代价过高。建议新增一个只倒序读尾部（如 64KB）的 IPC，一次返回 `{ mtimeMs, lastStopReason }`。`readSessionMessages` 的返回值本身已含 `stopReason`（`ReplayMessage extends AgentMessage`，`rpc-types.ts:86/107`），所以判据 `stopReason === 'stop'` 是拿得到的。✅

### 3.3 P1 的覆盖边界要说清（不是「终极兜底」）

omp **只在 agent_end 才 flush JSONL**（`store.ts:1188`、`App.tsx:432` 注释均为实证）。因此：

- 「JSONL 有 `stop:'stop'` 收尾」⟹ turn 确实完结 —— 判据可靠；
- 但反向不成立：帧流断了且 omp 没走到 flush → JSONL 无收尾 → **P1 不触发**。

本次事故中 omp 日志有完整总结（`stopReason=stop`），所以 P1 对**这一例**有效；但文档把它写成「覆盖 exit 丢 + RPC 挂的一切组合」是过头了。它覆盖的是「omp 端已完成落盘、只是帧没送到」这一类。

### 3.4 行数估计偏乐观

P1 原文 ~40 行。实际需要：`ipc-channels.ts`（常量 + `OmpApi` 签名）、`preload.ts`（桥接）、`main.ts`（handle）、`session-store.ts`（尾部读取）、`App.tsx`（看门狗分支）≈ **60+ 行**。P0 原文 ~60 行，实际 6 文件 ≈ **90 行**。

---

## 四、P2：判据自依赖，灰橙点在事故态下不会亮

方案 5 说「`isStreaming && 静默>10min` → 灰橙点 + 疑似卡死」。看 `stuckSince` 是怎么写的：

- 只在 `reconcileStreamingState` **成功拿到 `getState`** 之后写入（`App.tsx:1559-1560`）；
- 看门狗只在 `status === 'online'` 时调它（`App.tsx:770-773`）。

而事故态正是「status 卡 online、`getState` 失败」→ `stuckSince` **永远写不进去** → 灰橙点永远不亮。**P2 依赖的那条路径，就是 P0 要修的那条失效路径。**

好消息：非 online 分支（`App.tsx:776-784`）用的是 `lastFrameAt` 而不是 `stuckSince`，逻辑是对的。

**修正**：P2 的判据改用 `lastFrameAt`（或 3.2 里那个 IPC 的 `mtimeMs`），不要挂在 `stuckSince` 上。同时把 `stuckSince` 的写入挪到「对账失败」也能触发的位置，否则它作为「疑似卡死」标记名不副实。

改动面本身很小：`SessionDot` 联合类型（`store.ts:546`）、`SESSION_DOT_TITLES`（:557）、`SessionList.tsx:93` 的 `className`/`title`、`styles.css:564-566`。可行。✅

---

## 五、P3：有个前置 bug 没列，不修则 P3 空转

`automationRuns` 的 key 是 **tempKey**（`App.tsx:962-963`），而 `migrateTempSession` 迁移了 `sessionsMap` / `procStateMap` / `uiQueue` / `sessionNames` / `lastModelMap` / 状态点标记（`App.tsx:289-322`），**唯独没有迁移 `automationRuns`**。

后果：temp→real 迁移一发生（首条消息 `agent_end` 就会触发，`App.tsx:433`），`automationRuns.current.get(sessionPath)` 就再也查不到 → P3 的「超时检测」按 path 查必然查空 → **P3 完全空转**。

顺带：这个 bug 现在就已经存在——`onExit` 里的「记失败 run」（`App.tsx:475-486`）和 `agent_end` 里的「记成功 run」（`:435-443`）在迁移后同样查不到，即**定时任务的成功/失败记录现在就已经在丢**。值得单独开一条修。

P3 其余部分可行：`startedAt` 已有（`App.tsx:959`），记失败 run 的通道已有（`recordAutomationRun`）。✅

---

## 六、验收标准：第 2 条测不到目标路径

> 手测：杀掉运行中会话的 omp 进程树 → ≤90s 内侧栏解除「运行中」并提示失联

用 `taskkill /T /F` 杀掉 omp 进程树后，Node 的 `child.on('exit')` **必然触发** → `handleExit`（`omp-process.ts:315`）→ pool `onExit`（`omp-pool.ts:387`）→ `sendToRenderer(IPC.OmpExit)` → renderer 正常清理。

即：**正常杀进程走的是本来就成功的 onExit 老路径，新代码一行都不会被执行**。这条手测无论通过与否都不能证明 P0 有效。

**改成**（二者取一）：
1. 主进程临时屏蔽 `sendToRenderer(IPC.OmpExit)`（或在 devtools 里摘掉 `onExit` 订阅），再杀进程树 → 观察 ≤30s（修正版）/≤90s（原方案）内是否自愈；
2. 纯单测：`reconcileStreamingState` 在 `getState` 抛 `not online` / 返回 `{state:'dead'}` / 返回 `{state:'alive'}` 三种输入下的行为。

第 1、3 条（单测、tsc + vitest）没问题，`shouldHealStuckStreaming` 的纯函数测试范式在 `store.test.ts:499-527` 已有现成模板可套。✅

**另**：文档写「bump 0.5.20」，但 `package.json` 已经是 **0.5.20**（且文档自称基线 0.5.19）。应 bump **0.5.21**。

---

## 七、建议的落地顺序（修正后）

| 优先级 | 动作 | 位置 | 量级 |
|---|---|---|---|
| P0-1 | `catch` 按错误分类：`not online` → 直接 `clearSessionProc`；其余计数 | `App.tsx:1567` | ~15 行 |
| P0-2 | 抽 `clearSessionProc(path, reason)`（**不含** tempKey 迁移 / respawn 分支） | `App.tsx:487` | ~15 行 |
| P0-3 | `!r.success` 与计数达阈 → 同样调 `clearSessionProc` | `App.tsx:1541` | ~5 行 |
| P1-1 | 新增尾部探针 IPC（mtime + lastStopReason），倒序读 64KB | `session-store.ts` / `main.ts` / `preload.ts` / `ipc-channels.ts` | ~40 行 |
| P1-2 | 看门狗分支：**先清 isStreaming → 再 loadSessionMessages** | `App.tsx:763` | ~15 行 |
| 顺手修 | `migrateTempSession` 同步迁移 `automationRuns` 的 key | `App.tsx:289` | ~8 行 |
| P2 | `sessionDotStatus` 加「静默」态，判据用 `lastFrameAt`/mtime 而非 `stuckSince` | `store.ts:566` / `SessionList.tsx:93` | ~25 行 |
| 后置 | 主进程探针 IPC（三态，查 `exitCode`/`signalCode`） | `omp-pool.ts` / `omp-process.ts` | ~30 行 |
| 后置 | P3 自动化超时 abort | `App.tsx:951` | ~30 行 |
| 后置 | 对账用 `getState` 传短超时（3s），避免 5min pending 堆积 | `rpc-client.ts:32` | ~3 行 |

**待查项**保留，但建议把「PermissionModal approve 路径断帧流」从待查清单降权（见 §1 反证），重心放到「agent_end / exit 帧在传送层的丢失层级」——主进程未发 / webContents 未达 / renderer handler 未执行。

---

## 附：核对过的文件

`src/renderer/App.tsx`（看门狗 763-789、onExit 473-542、reconcile 1533-1570、migrateTempSession 208-324、automationRuns 948-995、onSend 649-650）
`src/renderer/store.ts`（ProcState 169、sessionDotStatus 566、shouldHealStuckStreaming 583、loadSessionMessages 1182-1260、setProcState 1161）
`electron/omp-pool.ts`（get 92、send 204、evict 219、onExit 373-388、entries.delete 379）
`electron/omp-process.ts`（handleExit 315、kill 340、pid/isRunning 149-156）
`electron/main.ts`（RpcSend 424-445、pool events 901-908）
`src/main/frame-router.ts`（默认超时 5min, :8）
`src/main/session-store.ts`（mtime 155、readSessionMessages 221）
`src/renderer/rpc-client.ts`（__rpcError 拆包 25-27）
`src/shared/rpc-types.ts`（stopReason 86、ReplayMessage 107）
`src/renderer/components/SessionList.tsx`（dot 渲染 81-93）、`store.test.ts`（纯函数测试范式 499-527）
