# Issue：会话卡「运行中」25 小时（bet_zp 01a0cc6c 事故复盘 + 修复方案）

日期：2026-09-25　版本基线：0.5.19　状态：方案待实施

## 一、事故还原

会话 `01a0cc6c`（bet_zp，9/23 12:01 自动化拉起）：

| 时间 | 事件 |
|---|---|
| 9/23 21:27:08 | agent 调 `"C:\Users\17593\.workbuddy\binaries\...\python.exe" jc_pull.py` → project-guard 弹权限审查 |
| 9/23 21:27:15 | 用户点同意，命令成功返回，回合继续 |
| 9/23 21:42:45 | turn **服务端正常收尾**（omp 日志 stopReason=stop，JSONL 有完整总结，无报错无重试） |
| 9/23 21:42 → 9/24 22:51 | UI 一直显示「运行中」约 25h，期间无任何内容渲染 |
| 9/24 22:51:40 | 用户发 `?`，以 `steering:true` 落盘（UI 仍认为在跑）；由**新 spawn 进程**（pid 42332，resume）处理，历史重载一次性吐出积压内容 |

附属问题（已修）：SKILL.md 硬编码用户目录 python/node 绝对路径 → 触发 project-guard 审查。已改用默认 PATH 版本。

## 二、根因：自愈防线的结构性盲区

现有三层防线全部失灵，关键是第三层有个静默死循环：

| # | 防线 | 位置 | 失灵原因 |
|---|---|---|---|
| 1 | agent_end 帧复位 isStreaming | `store.ts` applyAgentEvent | 帧流在**权限批准后断裂**（断点待查 PermissionModal approve 路径），agent_end 未到达渲染层 |
| 2 | onExit 清理（offline + isStreaming:false） | `App.tsx` onExit | **exit 事件也丢了**（进程 40436 消失但事件未到） |
| 3 | 30s 看门狗对账 | `App.tsx reconcileStreamingState` | **盲区**：进程死后 `rpc.getState` 抛错/失败，走 `catch{}`（:1567）与 `if (!r.success) return`（:1541）——两处都**静默 return**，注释假设"交给 onExit"，但 onExit 恰恰丢了 → 每 30s 失败一次、静默放弃一次，无限循环 |

本质：防线 3 的设计前提是「onExit 必达」，而 exit 事件并不保证送达（sleep 唤醒、管道断裂、bun shim 异常均可丢）。

## 三、修复方案

### P0 补对账闭环（修根洞，~60 行）
1. `reconcileStreamingState` 的 `catch` / `!r.success` 分支改为**失败计数**（procState 加 `reconFailCount`）；连续 ≥3 次（约 90s）→ 调主进程探针。
2. 主进程新增 IPC `omp:probe-session`：按 sessionPath 检查 ChildProcess 实况（pid 存活 + exit 是否已挂），返回 `{alive, pid}`——不依赖 omp 配合的独立真相源。
3. 探针报死 → 抽取 onExit 的清理为公共函数 `clearSessionProc(path, reason)` 复用：复位 isStreaming、侧栏标离线、toast 提示「进程失联，发消息将自动重启」；下次 prompt 走既有 acquire 路径（本次事故 pid 42332 已验证可行）。

### P1 jsonl 磁盘对账（终极兜底，~40 行）
4. 看门狗对 `isStreaming && 静默>10min` 的会话，读 `<sessionPath>.jsonl` mtime + 尾部：**mtime 距今 >10min 且最后一条 assistant 消息 `stopReason:'stop'` → 判定 turn 必然已完结** → 清 isStreaming + `loadSessionMessages` 重载。覆盖「exit 丢 + RPC 挂」的一切组合（含进程僵死但 JSONL 已写完收尾）。

### P2 显示语义（~20 行）
5. `sessionDotStatus` 区分「运行中（有帧）」vs「运行中但静默>10min」：后者灰橙点 + 「疑似卡死」提示，不让用户对假"运行中"干等。

### P3 自动化加固（可选后置）
6. 自动化 prompt 记 startedAt；超时（如 6h）无帧且无 agent_end → 自动 abort + 记失败 run。

### 待查（本次遗留）
- PermissionModal approve 应答路径为何断帧流（防线 1 断点）。
- exit 事件丢失的层级：omp 进程未触发 / 主进程未转发 / 渲染层未收到。

## 四、验收

- 单测：`shouldHealStuckStreaming` 同款纯函数覆盖 reconFailCount 判定与 jsonl 收尾判定。
- 手测：杀掉运行中会话的 omp 进程树 → ≤90s 内侧栏解除「运行中」并提示失联。
- 回归：`npx tsc --noEmit` + vitest 全量通过，bump 0.5.20。


## 五、评审意见核对（20260925_review.md，已逐条对代码验证）

| 评审论点 | 核对结果 |
|---|---|
| §2.1 探针查池会误报「活着」（entries 只在 exit/evict/spawn-fail 删除） | ✅ 对（omp-pool.ts:92-94/379）。探针必须查 exitCode/signalCode 三态 |
| §2.2 catch 按「not online」错误分类，直接清理，无需新 IPC | ✅ 对且优于原方案（omp-pool.ts:206 立即 throw；main.ts:442 包 __rpcError）。事故场景（主进程已知死、renderer 丢事件）直接命中，≤30s 恢复 |
| §2.3 90s 计数不成立（FrameRouter 默认 5min 超时，会堆 pending） | ✅ 对（frame-router.ts:8）。计数路径需 3s 短超时 |
| §2.4 抽 clearSessionProc 可行，但不得含 tempKey 迁移/respawn 分支 | ✅ 对 |
| §3.1 必须先清 isStreaming 再 loadSessionMessages，否则重载被 :1199 守卫丢弃 | ✅ 对，P1 成败关键 |
| §3.2 mtime 白嫖 sessions 列表；尾部 stopReason 用 64KB 倒序轻量 IPC，勿全量读 | ✅ 对（session-store.ts:155；全量读每 30s 代价过高） |
| §3.3 P1 只覆盖「omp 已落盘收尾」类，非「一切组合」 | ✅ 对，原文过头。omp 仅 agent_end flush |
| §4 P2 判据挂在 stuckSince 会自依赖失效（事故态写不进去），改用 lastFrameAt | ✅ 对（App.tsx:1559 stuckSince 仅 getState 成功路径写入） |
| §5 automationRuns 按 tempKey 记录、migrateTempSession 不迁移它 → 现存丢记录 bug | ✅ 对（App.tsx:963 set(tempKey)；迁移列表无此项），单独修 |
| §6 验收 2 杀进程树走 onExit 老路径测不到新代码；版本应为 0.5.21 | ✅ 对（package.json 已 0.5.20） |
| §1 「approve 断帧流」与「后续 15 分钟正常工作」自相矛盾 | ⚠️ **论据不成立**：混淆了「渲染层帧订阅断裂」与「omp 服务端停止工作」，两者可并存；用户证词（同意后无任何回应+积压靠重载吐出）恰恰支持断点在 approve 附近。但「传送层级三层排查」作为待查方向本身合理，可并行保留 |

**结论**：11 条实质意见 10 条正确且有代码实证，采纳评审的落地顺序（错误分类 ~25 行先行，探针后置只覆盖「entry 在但僵死」窄场景）；§1 的反驳论据有逻辑错误，待查项保留两个方向并行。
