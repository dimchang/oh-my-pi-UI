# 20260914 问题报告：会话串台 & jsonl 暴涨

- 报告日期：2026-09-14（GMT+8）
- 项目：OMP-UI（Electron 38 + React 18 + TS 5.6，对接 oh-my-pi）
- 涉及会话文件：`C:\Users\17593\.omp\agent\sessions\--D--code-OMP-UI--\2026-09-13T16-46-00-813Z_01a09ba9-546d-7423-88a6-746d2343197f.jsonl`（下称 **会话 A**）
- 环境：omp `18.0.4`（`C:\Users\17593\.bun\bin\omp.exe`）；排查期间 OMP-UI 应用重启过一次
- 排查范围：未改动任何项目源码、未改动任何 omp 配置

---

## 摘要

| # | 用户描述 | 定性 | 结论 |
|---|---------|------|------|
| 1 | 在「另一个新会话」里问的问题，串到了会话 A 的显示里；继续提问后这个显示又不见了 | **UI 侧「当前会话指针」竞态** | 不是 omp 跨会话泄漏、也没有数据丢失。发送时 `currentSessionPath` 仍指向会话 A，prompt 被真实地发给了 A 的进程；新建的那个 temp 会话进程自始至终**没收到任何 prompt**，因此磁盘上不存在它的 `.jsonl` |
| 2 | 只问了一个问题，jsonl 就 5MB+，内容里有大量「乱码」/像 SDK key 的东西 | **omp `snapcompact` 压缩产物** | 不是乱码、不是密钥，是 omp 18.x 把旧历史渲染成 PNG 后以 base64 写进 `compaction.preserveData.snapcompact.frames[].data`。真正的问题是它**每 ~2 分钟压一次、且压缩后上下文反而变大**，导致 27 分钟内 11 次压缩 / 55 张 PNG / 累计丢弃 75.7 万字符历史 |

---

# 问题 1：新会话的提问「串」到旧会话

## 1.1 现象（用户原描述）

> 我在另外一个新开会话（`__new_a4d70e65-c1a9-4ae5-a8a5-dfc1d1a2345e`）里面说的「1.你研究下 OMP 项目…2.针对这些改动…需要改动吗？」，为什么串到这个 session 来了？我在继续问了新的问题后，这个其他对话串过来的问题显示，又不见了。

## 1.2 证据链（4 条，全部可复现）

| # | 检查项 | 方法 | 结果 |
|---|-------|------|------|
| E1 | 该提问文本在全盘的分布 | 递归扫描 `~/.omp/agent/sessions`（228 个 `.jsonl`/日志）、`%APPDATA%/omp-gui`、项目 `.temp`，匹配 `18.0.1到现在` / `除了修复bug以外` / `can1357/oh-my-pi的18` | **只命中会话 A 一处**：`type:"message"`, `id=13efee4d`, `parentId=c5c45f54`, `ts=2026-09-13T17:07:35.523Z`（行 #355）。没有第二个副本 |
| E2 | 会话 A 的 jsonl 是不是被两个进程同时写 | 统计 entry 链完整性：重复 `id`、`parentId` 分叉、`parentId` 指向不存在 id | **重复 id = 0；孤儿 parentId = 0；分叉仅 1 处**（`parent=20d67a5d` 有 `message`+`custom_message` 两子，属 omp 正常的 async-result 结构）⇒ **单一写入者**，没有第二个进程往 A 里追加 |
| E3 | 无 `-r`/`-c` 启动的 rpc-ui 进程会不会「挂到最近的会话」上 | 探针 `.temp/probe-fresh2.mjs`：以 `cwd=D:\tmp`、参数 `--mode rpc-ui --approval-mode yolo`（与 `acquireNew` 完全一致）spawn，然后发 `get_state` / `get_messages` | `sessionFile` = **全新文件**（`...\--D--tmp--\2026-09-13T17-26-59-442Z_01a09bce-d872-7119-820f-d620defb5341.jsonl`）、`sessionId=01a09bce-d872-7119-820f-d620defb5341`、`get_messages=0` ⇒ **不会**挂到最近会话上。故那个 `__new_…` 进程不可能把 prompt 写进会话 A |
| E4 | temp 会话是否在别处落盘过 | 扫描最近 3 小时内所有 session 目录的新增文件/目录（`.temp/recent.mjs`） | 除会话 A 自身的 jsonl 与其工具日志目录外，**无任何新增**；`__new_a4d70e65…` 全盘无对应 `.jsonl` |

### E1 的具体记录

```
行#355  type=message  id=13efee4d  parentId=c5c45f54  ts=2026-09-13T17:07:35.523Z
message.role = "user"，attribution = "user"
content = "1.你研究下OMP 项目https://github.com/can1357/oh-my-pi的18.0.1到现在最新版本，
           除了修复bug以外的改动有多少？2.针对这些改动，对我们这个项目有什么影响吗？需要改动吗？"
```

会话 A 的全部真实用户消息（共 5 条）——说明那次提问在 A 里就是一条**正常的用户回合**：

| 行# | 时间(UTC) | 文本 |
|-----|-----------|------|
| 10 | 16:52:02 | 我的 glm-5.3-flash 模型支持 image，但是我的 UI 还是弹出了 "inspect_image is now available…" |
| 64 | 16:53:32 | 你先分析原因，不要改文件先 |
| 306 | 17:05:30 | 我已经说过了，不要改文件，不要改文件… |
| **355** | **17:07:35** | **1.你研究下 OMP 项目…需要改动吗？（本次被质疑「串台」的那条）** |
| 687 | 17:18:00 | 我问的最后一句话是什么？ |

> 补充：排查时的进程快照显示当时有 4 个 omp 进程，其中 2 个是**无 `-r` 的空新会话进程**（`omp.exe --mode rpc-ui --approval-mode yolo --hook …project-guard.ts`），另 2 个分别 `-r …16-46-00…jsonl`、`-r …2026-09-12T11-57-48…jsonl`。即「temp 进程存在但从未落盘」，与 E4 一致。

## 1.3 根因

**prompt 实际被发给了会话 A 的 omp 进程** ⇒ 发送瞬间渲染层的 `currentSessionPath` 仍是 A。
这是**渲染层「当前会话指针」与「用户以为的当前会话」不一致**的竞态，不是 omp 侧问题。

可复现的 4 个入口：

### (1) 新建会话存在 ~2.8 秒「空窗期」，期间输入框照常可用

`src/renderer/App.tsx:612-640`（`doNewSession`）：

```ts
knownSessionPathsBeforeNew.current = new Set(...);              // 630
const { sessionPath } = await rpc.newSessionForCwd(...);         // 633  ← 等 omp 进程 ready
await resolveAndSelectNewSession(sessionPath, targetCwd);        // 634  ← 才切指针（App.tsx:240）
```

`resolveAndSelectNewSession`（`App.tsx:239-250`）里才执行 `setCurrentSessionPath(newSessionPath)`。

**实测窗口（`.temp/probe-time.mjs`，3 次）**：spawn→`ready` 帧 = **2860 / 2951 / 2524 ms（均值 2778 ms）**。
在这 ~3 秒内：界面仍是会话 A、输入框可用、`currentSessionPath === A`。此时回车 → `onSend` 取到 `sp = A` → prompt 进 A 的 jsonl。这正是 E1 记录的那条消息。

### (2) 启动时的「自动选中」会无条件抢回 mtime 最新的旧会话

`src/renderer/App.tsx:643-674`：

```ts
const newest = st.sessions.filter(x => cwdKey(x.cwd) === cwdKey(cwd))
                          .sort((a, b) => b.mtime - a.mtime)[0];   // 655
if (newest) {
  st.setCurrentSessionPath(newest.path);                            // 659 ← 无条件切
  st.loadSessionMessages(newest.path);                              // 660
```

该 effect 只判断「有没有会话」，**不判断用户当前是否已经在一个（新）会话里**。配合下一条，就出现「重启后回到 A、并且找不到那个 jsonl」。

### (3) 空 temp 占位会被静默丢弃（占位只活在内存里）

- `src/renderer/App.tsx:707-715`（`onSelectSession`）：`cur` 是 `__new_` 开头的 temp 会话且 `sessionsMap[cur]` 为空时，直接 `discardTempSession(cur)`——**释放进程 + 从侧栏移除占位**。
- 判据是「渲染层缓冲为空」，但缓冲为空有两种含义：① 用户确实没发过；② 消息**错投到了别的会话**（本 bug 正是 ②）。于是 temp 会话被判为「空」而被清掉。
- `src/renderer/App.tsx:679-705`（`discardTempSession`）只释放进程与 store 条目；`__new_*` 占位**从未写入 `workspaces.json`**，进程重启即全部消失 ⇒ 用户事后「找不到对应的 JSONL 文件」。

### (4) `sp` 快照与 `appendUserMessage` 各自读 `currentSessionPath`，可失配

`src/renderer/App.tsx:454-482`（`onSend`）：

```ts
let sp = st.currentSessionPath;                                   // 456  进入时快照
const doSend = async () => {
  const imageRefs = await collectImageRefs(attachments);
  await rpc.acquire(sp!, cwd, approvalMode);                      // 474
  useApp.getState().appendUserMessage(text, { attachments });     // 475  ← 内部重新读 currentSessionPath
  await rpc.prompt(sp!, promptText, imageRefs);                   // 476  用快照 sp
```

`appendUserMessage`（`src/renderer/store.ts:1036`）内部是 `const path = st.currentSessionPath ?? ''`，与 456 行的 `sp` **不是同一个数据源**，两次 await 之间指针变化就会让「气泡进 X、prompt 进 Y」。
（`onGuide` 505-506、`onQueue` 529-530 同样问题。）

### 关于「继续提问后又不见了」

磁盘上那条消息**一直在**会话 A 的 jsonl 里，所以不是数据丢失或再次错投，而是**视图被切走**：空窗期结束后 `resolveAndSelectNewSession` 会 `resetChat()`（`App.tsx:242`），界面切到那个**空的**新会话；此后切会话时 `messages` 又由 `loadSessionMessages(path)` 重建。这半段我只能给出候选机制，要唯一钉死需要现场读渲染层状态（见 1.5）。

## 1.4 解决方案（建议改动清单，按优先级）

> 本次未改任何代码。

| 级别 | 改动 | 位置 | 说明 |
|------|------|------|------|
| **P0** | 消除空窗期：把 tempKey 的生成提前到渲染层 | `electron/main.ts:365-377`、`src/renderer/App.tsx:612-640` | 渲染层先 `crypto.randomUUID()` 组出 `__new_<uuid>`，**先** `setCurrentSessionPath(tempKey)` + `resetChat()` + `upsertSessionPlaceholder()`，**再** `await` spawn（主进程 `acquireNew(tempKey, …)` 签名无需改）。指针在 spawn 前就已切走，空窗期不存在 |
| **P0** | spawn 期间给输入框加禁用/加载态（双保险） | `src/renderer/components/InputBox.tsx` + `App.tsx` | 即使还有别的时序分支，也不会把消息投给旧会话（现有 `creatingSession` 只防重入，不冻结输入） |
| **P0** | `appendUserMessage` 显式传 `sp` | `App.tsx:475 / 506 / 530`、`store.ts:1036` | 签名加 `sessionPath` 参数，禁用内部重读 `currentSessionPath`，杜绝「气泡与 prompt 分家」 |
| **P0** | 收窄静默丢弃判据 | `App.tsx:707-715`、`679-705` | 「缓冲为空」→ 「该 temp 会话从未提交过」（新增 `hasSubmitted`/`lastSentAt` 标记，由 `onSend/onGuide/onQueue` 写入）。否则错投场景下新会话仍会被误清 |
| **P1** | 启动自动选中加护栏 | `App.tsx:643-674` | 仅在 `currentSessionPath` 为空时自动选；且存在 `__new_` 占位时不抢指针 |
| **P1** | 彻底废掉「靠 mtime+快照猜路径」的迁移 | `App.tsx:140-236`（`migrateTempSession`） | 首轮 `agent_end` 后直接 `rpc.getState(tempKey).sessionFile`（E3 已验证该字段准确）→ `renameKey(tempKey, sessionFile)`。不再需要 `knownSessionPathsBeforeNew`/`claimedRealPaths`/8 次重试这些启发式，从根上消除误迁风险 |
| **P1** | 打包版开放远程调试 | `electron/main.ts` | 便于现场诊断（见 1.5）。可只在 dev 或加环境变量开关 |

## 1.5 现场验证方法（复现/钉死用）

1. 打开 OMP-UI → 菜单里 `toggleDevTools`（`electron/main.ts:766`；**打包版未开 `--remote-debugging-port`**，我这次用 CDP 连不上，`%APPDATA%/omp-gui/DevToolsActivePort` 里的 9411 是旧 dev 实例残留）。
2. 在 Console 里读诊断环形日志：`window.__ompDiag`（帧路由/丢弃记录）。
3. 手动复现 P0 场景：点「新建会话」→ 立刻在输入框粘贴并回车 → 观察
   - 新会话侧栏占位是否出现、是否被清掉；
   - 该工作空间对应的 `~/.omp/agent/sessions/--<编码 cwd>--/` 下是否出现新的 `.jsonl`；
   - 旧会话 jsonl 末尾是否多出这条 user 消息（`grep -c` 计数即可）。

---

# 问题 2：jsonl 27 分钟内涨到 9.66MB，内容像乱码/密钥

## 2.1 现象

> 这个文件我只问了一个问题，它就变为 5M+ 大小了，似乎非常的不正常。

## 2.2 事实与体积归因

会话 A 文件当前 **9,661,331 字节（9.66 MB 磁盘）**、**899 行**。按行类型精确归因（byte 级，含 JSON 结构）：

| 类型 | 条数 | 体积 | 占比 |
|------|------|------|------|
| `compaction` | **11** | **5.63 MB** | 61% |
| └ 其中 `preserveData.snapcompact.frames[].data`（PNG base64，55 张） | 55 帧 | **4.27 MB** | 46% |
| └ 其中 `preserveData.snapcompact.text`（`textHead`+`textTail`） | 11 | 1.18 MB | 13% |
| └ 其中 `summary` 文本 | 11 | 0.01 MB | — |
| `message` | 546 | 3.44 MB | 37% |
| `custom`（`tool_execution_start`） | 325 | 0.14 MB | 2% |
| `model_change` / `custom_message` / `title` / `session` / `thinking_level_change` | 17 | < 0.01 MB | — |

> 排查期间该文件仍在增长（同一场对话还在跑）：开始排查时 7.32 MB / 714 行 / 9 次压缩，收尾时 9.66 MB / 899 行 / 11 次压缩。上表为收尾值。

**「乱码/SDK key」就是那 55 张 PNG 的 base64**，字段结构（去掉 data 后）：

```json
{ "mimeType": "image/png", "cols": 196, "rows": 71, "chars": 13141,
  "font": "8x13", "variant": "bw", "lineRepeat": 1, "detail": "original",
  "data": "<base64 95,956 chars>" }
```

即：把历史记录**渲染成 196 列 × 71 行的位图页面**（一页约 13,141 字符，实心黑格代表换行），再 base64 存进 jsonl，同时作为压缩后的上下文交给模型。

## 2.3 压缩行为明细（11 次，跨度 16:56:53 → 17:24:13 UTC，共 27.3 分钟）

| 行# | 时间(UTC) | tokensBefore → After | 帧数 | PNG base64 | totalChars | truncatedChars |
|-----|-----------|---------------------|------|-----------|-----------|----------------|
| 180 | 16:56:53 | 88,519 → 59,882 | 5 | 387 KB | 84,417 | 76,681 |
| 266 | 16:59:38 | 59,700 → **72,104 ↑** | 5 | 382 KB | 83,789 | 172,359 |
| 317 | 17:06:06 | 57,401 → **68,283 ↑** | 5 | 329 KB | 76,021 | 249,196 |
| 394 | 17:08:32 | 67,956 → 63,567 | 5 | 493 KB | 92,394 | 286,733 |
| 458 | 17:10:14 | 76,493 → 64,418 | 5 | 498 KB | 97,023 | 322,429 |
| 526 | 17:12:50 | 63,516 → **67,657 ↑** | 5 | 395 KB | 86,893 | 397,457 |
| 592 | 17:14:45 | 63,213 → **65,332 ↑** | 5 | 375 KB | 83,444 | 454,569 |
| 651 | 17:17:15 | 60,628 → **66,585 ↑** | 5 | 306 KB | 70,723 | 550,024 |
| 719 | 17:20:14 | 60,883 → **65,641 ↑** | 5 | 438 KB | 89,318 | 606,566 |
| 784 | 17:22:19 | 66,557 → **67,455 ↑** | 5 | 415 KB | 89,585 | 662,727 |
| 830 | 17:24:13 | 66,263 → 64,684 | 5 | 360 KB | 78,362 | **756,769** |

两个关键异常：

1. **压缩没起作用（空转）**：11 次里 **7 次 `tokensAfter > tokensBefore`**，上下文在 57k~88k 之间反复震荡，压完仍高于阈值 ⇒ 每约 2 分钟再压一次，写完就忘不掉。
2. **历史在被丢弃**：`truncatedChars` **单调递增到 756,769**。最后一次 compaction 的 `summary` 原文：

   > About 756769 characters of older middle history dropped to fit archive budget.

   即模型当前可见的「归档区」之外，已有约 75.7 万字符的中间历史被丢掉。这直接解释了用户随后那轮提问——用户问「我问的最后一句话是什么？」，agent 的思考链里出现 *"the HISTORY appears compressed/edited … Let me check the actual session JSONL to see the most recent user messages"*，**它是在翻磁盘猜答案**。

## 2.4 机制（为什么选到 snapcompact，为什么这么频繁）

`omp config list`（全量 501 行已存 `.temp/omp-config-list.txt`）相关项：

```
compaction.enabled = true
compaction.midTurnEnabled = true                                   ← turn 中途也压
compaction.methodOrder = ["remote","snapcompact","handoff","shake","soft"]
compaction.remoteEndpoint = (not set)                              ← remote 不可用
compaction.thresholdTokens = -1 / thresholdPercent = -1            ← 阈值自动
compaction.keepRecentTokens = 20000
compaction.v2RetainedMessageBudget = 64000
compaction.asyncEnabled = true
snapcompact.toolResults = false / systemPrompt = none / shape = auto
display.collapseCompacted = true                                   （仅 TUI）
```

- `methodOrder` 里 `remote` 排第一但 `remoteEndpoint` 未配置 ⇒ 永远落到 **第 2 位的 `snapcompact`**（图像化压缩）。
- `midTurnEnabled = true` ⇒ 长回合中途也触发，这才有 27 分钟内 11 次。
- 每次压缩都重新渲染整段历史 → 新增 5 张 PNG + 头尾各 13,916 字符文本（恰好对应 `shortSummary` 里的 "+27,833 chars as text"）。

## 2.5 解决方案

> 本次**未执行**以下任何命令（未改动的真实配置：`compaction.methodOrder` 仍是默认值）。数组值语法已在隔离 profile 中验证通过（`omp --profile=cfgprobe config set …` 后 `config get` 回读正确，真实 `config.yml` 未被写；探针 profile 已删除）。

**方案 A（推荐）——换掉图像压缩，保留压缩能力：**

```bash
omp config set compaction.methodOrder '["handoff","shake","soft"]'
omp config set compaction.midTurnEnabled false
```

**方案 B——进一步降频（可选，配合 A）：**

```bash
omp config set compaction.thresholdTokens 120000     # 抬高触发线，默认 -1 = 自动
omp config set compaction.keepRecentTokens 40000     # 多保留近期上下文
```

**方案 C——彻底关掉自动压缩（最激进）：**

```bash
omp config set compaction.enabled false              # 改为手动 /compact
```

**注意：**

1. omp **只在进程启动时读配置** ⇒ 改完必须重启 omp 进程（重启 OMP-UI 应用即可）才生效。
2. 生效判据：新产生的 compaction 条目 `method` 不再是 `snapcompact`、且 `preserveData` 里**没有 `frames`**。
3. **已写进旧 jsonl 的 55 张帧不要手工删**——`preserveData` 是模型上下文来源，手删等于破坏归档。等新配置生效后，新会话不会再长；旧会话若确实不再需要续接，可直接删除该 `.jsonl`（等于弃用历史，需你确认）。
4. 与 omp 的 `gc.archive / gc.coldArchiveAfterDays` 无关（那是 gc 用的）。

---

# 附录

## A. 本次使用的探针脚本（均在 `D:\code\OMP-UI\.temp\`，可复跑）

| 脚本 | 用途 |
|------|------|
| `probe-fresh2.mjs` | 无 `-r/-c` spawn `rpc-ui`，用 `get_state`/`get_messages` 证明「必拿全新空会话」 |
| `probe-time.mjs` | 量测 spawn→`ready` 耗时（新建会话空窗期） |
| `chain.mjs` | jsonl entry 链完整性（重复 id / 分叉 / 孤儿 parentId） |
| `an1.mjs`…`an8.mjs` | jsonl 结构、类型统计、大小归因、全盘文本搜索 |
| `breakdown.mjs` | 按行类型做 byte 级体积归因（本报告 2.2 表的数据源） |
| `verify2.mjs` | 行数 / compaction 数 / `tokensAfter>tokensBefore` 次数 / 帧数 / 时间跨度 |
| `recent.mjs` | 最近 N 小时 session 目录落盘扫描 |
| `comp-detail.mjs` | 逐个 compaction 的 tokens/帧/totalChars/truncatedChars |
| `procs.mjs` / `proctime.mjs` | omp 进程清单与命令行（判断每个进程绑定的会话） |
| `omp-config-list.txt` | `omp config list` 全量输出（501 行） |

## B. 关键命令

```bash
# 会话文件位置（--<编码 cwd>-- 目录下）
ls ~/.omp/agent/sessions/--D--code-OMP-UI--/

# omp 进程各自绑定哪个会话（有 -r 的就是被 resume 的会话）
#   实测样本：
#   omp.exe --mode rpc-ui --approval-mode yolo -r ...\2026-09-13T16-46-00-813Z_....jsonl --hook ...\project-guard.ts
#   omp.exe --mode rpc-ui --approval-mode yolo --hook ...                              ← 无 -r = 空新会话

# 该会话的用户消息（含被质疑"串台"的那条）
# grep -n '"role":"user"' <jsonl>

# 压缩相关可调项
omp config list | grep -i "compact\|snap"
```

## C. 未闭合点（诚实标注）

1. 问题 1「继续提问后显示又不见了」的**唯一**成因尚未钉死，仅确定为「视图被切走」（`resetChat` 到空的新会话 / 切会话时重建 `messages`），需现场开 DevTools 读 `window.__ompDiag` + store 快照才能收敛到一行证据。
2. 问题 1 中 `__new_a4d70e65…` 的进程究竟是「空窗期内收到 Enter 但消息发去了 A」还是「先被 (3) 静默丢弃」，两个入口我都给了代码依据；按 E1/E3/E4 推断前者更可能（temp 进程在排查时仍存活，说明未被 `discardTempSession` 释放）。
3. 问题 2 的 `tokensAfter > tokensBefore` 只做了现象统计，未深入 omp 内部对图像块 token 的计量实现（omp.exe 为 150MB 单文件编译产物，字符串不可直接检索）。
